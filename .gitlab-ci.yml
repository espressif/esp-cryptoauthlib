stages:
  - prepare
  - build

variables:
  PEDANTIC_FLAGS: "-Werror -Werror=deprecated-declarations -Werror=unused-variable -Werror=unused-but-set-variable -Werror=unused-function"

# First job to determine the latest ESP-IDF release
determine_latest_release:
  stage: prepare
  image: alpine:latest
  tags:
    - prepare
  script: |
    apk add --no-cache curl jq
    LATEST_RELEASE=$(curl -s https://api.github.com/repos/espressif/esp-idf/releases/latest | jq -r '.tag_name')
    if [ -z "$LATEST_RELEASE" ] || [ "$LATEST_RELEASE" = "null" ]; then
        echo "Error: Failed to determine latest ESP-IDF release"
        exit 1
    fi
    echo "LATEST_RELEASE=${LATEST_RELEASE}" >> build.env
    echo "Found latest ESP-IDF release: ${LATEST_RELEASE}"
  artifacts:
    reports:
      dotenv: build.env

.setup_idf_template: &setup_idf
  before_script:
    - . ${IDF_PATH}/export.sh
    - |
      if [[ "${IDF_VER}" == "release-v4.3" ]]; then
        pip install idf-component-manager==1.2.3 'idf-build-apps==1.1.5'
      elif [[ "${IDF_VER}" == "release-v4.4" ]]; then
        pip install idf-component-manager==1.2.3 'idf-build-apps==1.1.5'
      elif [[ "${IDF_VER}" == release-v6* || "${IDF_VER}" == "latest" || -z "${IDF_VER}" ]]; then
        # ESP-IDF 6.x sets __COMPONENT_MANAGER_INTERFACE_VERSION=5, which requires idf-component-manager>=3.0.
        # Jobs without IDF_VER use the `latest` or dynamically-resolved release image, which is 6.x today.
        pip install --upgrade 'idf-component-manager>=3.0' 'idf-build-apps>=2.4,<4'
      else
        # ESP-IDF 5.x pins idf-component-manager~=2.2 in requirements.core.txt, so 3.x is rejected at runtime.
        pip install --upgrade 'idf-component-manager<3.0' 'idf-build-apps>=2.4,<2.5'
      fi
    - export EXTRA_CFLAGS="${PEDANTIC_FLAGS} -Wstrict-prototypes"
    - export EXTRA_CXXFLAGS="${PEDANTIC_FLAGS}"

# Build for ESP32 on all IDF versions
build:esp32-all-versions:
  stage: build
  tags:
    - build
  parallel:
    matrix:
      - IDF_VER: ["release-v4.3", "release-v4.4", "release-v5.0", "release-v5.1", "release-v5.2", "release-v5.3", "release-v5.4", "release-v5.5", "release-v6.0", "latest"]
  image: espressif/idf:${IDF_VER}
  <<: *setup_idf
  script:
    - idf-build-apps build --target esp32 --paths examples/atecc608_ecdsa || exit 1
  allow_failure: false
  artifacts:
    when: always
    paths:
      - examples/atecc608_ecdsa/build/bootloader/bootloader.bin
      - examples/atecc608_ecdsa/build/partition_table/partition-table.bin
      - examples/atecc608_ecdsa/build/*.bin
      - examples/atecc608_ecdsa/build/flasher_args.json
      - examples/atecc608_ecdsa/build/config/sdkconfig.json
    expire_in: 1 week

# Build for all targets on latest ESP-IDF release
build:all-targets-latest-release:
  stage: build
  needs:
    - job: determine_latest_release
      artifacts: true
  tags:
    - build
  image: espressif/idf:${LATEST_RELEASE}
  <<: *setup_idf
  script:
    - echo "Building with ESP-IDF ${LATEST_RELEASE}"
    - |
      for target in esp32 esp32s2 esp32s3 esp32c3 esp32c5 esp32c6 esp32h2 esp32p4; do
        echo "Building for $target"
        idf-build-apps build --target $target --paths examples/atecc608_ecdsa --build-dir build_${target} || exit 1
      done
  allow_failure: false
  artifacts:
    when: always
    paths:
      - examples/atecc608_ecdsa/build_*/bootloader/bootloader.bin
      - examples/atecc608_ecdsa/build_*/partition_table/partition-table.bin
      - examples/atecc608_ecdsa/build_*/*.bin
      - examples/atecc608_ecdsa/build_*/flasher_args.json
      - examples/atecc608_ecdsa/build_*/config/sdkconfig.json
    expire_in: 1 week


# Build for all targets on master branch of IDF
build:all-targets-master:
  stage: build
  tags:
    - build
  image: espressif/idf:latest
  <<: *setup_idf
  script:
    - |
      for target in esp32 esp32s3 esp32c2 esp32c3 esp32c5 esp32c6 esp32h2 esp32p4; do
        echo "Building for $target"
        idf-build-apps build --target $target --paths examples/atecc608_ecdsa --build-dir build_${target} || exit 1
      done
  allow_failure: false
  artifacts:
    when: always
    paths:
      - examples/atecc608_ecdsa/build_*/bootloader/bootloader.bin
      - examples/atecc608_ecdsa/build_*/partition_table/partition-table.bin
      - examples/atecc608_ecdsa/build_*/*.bin
      - examples/atecc608_ecdsa/build_*/flasher_args.json
      - examples/atecc608_ecdsa/build_*/config/sdkconfig.json
    expire_in: 1 week

build:ecu-firmware-all-targets-latest-release:
  stage: build
  tags:
    - build
  parallel:
    matrix:
      - IDF_VER: ["release-v5.5", "release-v6.0", "release-v6.1"]
  image: espressif/idf:${IDF_VER}
  <<: *setup_idf
  script:
    - unset EXTRA_CFLAGS
    - unset EXTRA_CXXFLAGS
    - echo "Building esp_cryptoauth_utility/firmware with ESP-IDF ${IDF_VER}"
    - |
      for target in esp32 esp32s3 esp32c2 esp32c3 esp32c5 esp32c6 esp32c61 esp32h2 esp32p4; do
        echo "Building for $target"
        idf-build-apps build --target $target --paths esp_cryptoauth_utility/firmware --build-dir build_${target} || exit 1

        echo "Generating ecu_firmware.bin from ELF using esptool.py for $target"
        esptool.py --chip $target \
          elf2image esp_cryptoauth_utility/firmware/build_${target}/ecu_firmware.elf \
          --output esp_cryptoauth_utility/firmware/build_${target}/ecu_firmware.bin || exit 1

        echo "Writing info.txt for $target"
        git_commit_sha=$(git -C $IDF_PATH rev-parse HEAD)
        bin_sha1=$(sha1sum esp_cryptoauth_utility/firmware/build_${target}/ecu_firmware.bin | awk '{ print $1 }')
        {
          echo "ESP-IDF Version: ${IDF_VER}"
          echo "Git Commit: ${git_commit_sha}"
          echo "ecu_firmware.bin SHA1: ${bin_sha1}"
        } > esp_cryptoauth_utility/firmware/build_${target}/info.txt
      done
  allow_failure: false
  artifacts:
    when: always
    paths:
      - esp_cryptoauth_utility/firmware/build_*/ecu_firmware.elf
      - esp_cryptoauth_utility/firmware/build_*/ecu_firmware.bin
      - esp_cryptoauth_utility/firmware/build_*/info.txt
      - esp_cryptoauth_utility/firmware/build_*/flasher_args.json
      - esp_cryptoauth_utility/firmware/build_*/config/sdkconfig.json
    expire_in: 1 week
